# flash-agents-wasm

Native Rust/PyO3 host extension used by the `flash-agents` Python SDK.

This package is normally installed as a dependency of `flash-agents`; users do
not need to install or import it directly.
